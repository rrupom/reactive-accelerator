import { useEffect } from "react";
import useSomeAPI from "../utils/useSomeAPI";

export default function Test8({ data }) {
  //   useEffect(() => {
  //     if (data) {
  //       onFetched(data);
  //     }
  //   }, [onFetched, data]);

  return <div>Example 8</div>;
}
